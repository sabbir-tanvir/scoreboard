/**
 * Backend connection config.
 *
 * When testing on a physical device, replace "localhost" with your
 * machine's LAN IP address (e.g. "192.168.1.42").
 *
 * When running on an Android emulator, use "10.0.2.2" instead of
 * "localhost" to reach the host machine.
 */
export const API_URL = "http://localhost:4000";
export const SOCKET_URL = "http://localhost:4000";
